# Mobile Ess

- Task ID: 20260520-161343-mobile-ess
- Status: active
- Created: 2026-05-20 16:13:43

## Goal

When the app connects to PCS, read PCS control protocol registers 1000 through 1024 and use/display those live read values in the PCS control UI.

## Scope

Included:

- Add read-only live control register value properties for protocol 1000-1024.
- Read control protocol values after PCS communication is established and during input register polling.
- Update the PCS control layout to show each live read value next to the corresponding input field.

Excluded:

- Refactoring PCS communication architecture.
- Changing PCS write/control command semantics.
- Changing DB, BMS, or dashboard behavior.

## Impacted Files

- EMS_PJT_Hamburger/Models/Client/PCS/PcsModel.cs
- EMS_PJT_Hamburger/ViewModels/PcsViewModel.cs
- EMS_PJT_Hamburger/Views/PCSView.xaml

## Test Strategy

- Unit: run Harness unit gate; current solution has no test projects, so warning pass is expected.
- Integration: run Harness integration gate; no integration test projects are expected.
- Static analysis: run Harness static gate.
- Build: build solution with MSBuild Release|Any CPU.
- E2E: run Harness release artifact verification.
- Manual check: verify PCS control panel labels bind to live read properties and default to safe placeholder values before communication.

## Rollback

Revert changes in `PcsModel.cs`, `PcsViewModel.cs`, and `PCSView.xaml`. The change is UI/read-only telemetry plus register reads, so rollback does not require DB/schema migration.

## Notes

- Harness guard must pass before app code edits.
